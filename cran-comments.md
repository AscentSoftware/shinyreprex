## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new release.

Notes:

- Updated the description field in the DESCRIPTION file from 'Shiny' to 'shiny'. In a
separate package release was told to keep the title field in title case so have left
as 'Shiny'
- Hard-coded the base packages as a vector rather than using `installed.packages()`
